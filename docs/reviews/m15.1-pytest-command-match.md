# M15.1 — Two Confirms Before M16

## 1. pytest command matches .github/workflows/ci.yml

**ci.yml pytest block (verbatim):**
```
      - name: Test with pytest
        run: |
          python -m pytest \
            tests/embed/ \
            tests/leads/ \
            tests/widgets/ \
            tests/middleware/ \
            tests/models/ \
            tests/repositories/ \
            tests/routers/ \
            tests/scrapers/ \
            tests/services/ \
            tests/test_main.py \
            tests/test_background_jobs.py \
            tests/test_db_schema.py \
            tests/test_e2e_widget.py \
            tests/test_lead_worker.py \
            tests/test_report_worker.py \
            tests/test_worker.py \
            --ignore=tests/test_e2e.py \
            --ignore=tests/test_ai_e2e.py \
            --cov=app \
            --cov-report=term-missing \
            --tb=short \
            -v
```

**Command actually run (flattened to one line):**
```
python -m pytest tests/embed/ tests/leads/ tests/widgets/ tests/middleware/ tests/models/ tests/repositories/ tests/routers/ tests/scrapers/ tests/services/ tests/test_main.py tests/test_background_jobs.py tests/test_db_schema.py tests/test_e2e_widget.py tests/test_lead_worker.py tests/test_report_worker.py tests/test_worker.py --ignore=tests/test_e2e.py --ignore=tests/test_ai_e2e.py --cov=app --cov-report=term-missing --tb=short -v
```

**Verdict:** Byte-identical (same paths, same flags, same order, backslash continuation removed as expected by the shell).

## 2. M12 commit `16fcc4b` — injection tests exist

```
git diff 16fcc4b^..16fcc4b -- tests/leads/test_router.py | grep -n "test_invalid_widget_id_format\|test_path_traversal_widget_id"
```

Output:
```
+    def test_invalid_widget_id_format(self, client):
+    def test_path_traversal_widget_id(self, client):
```

**Verdict:** Both tests were added in M12 for the `POST /public/widget/{id}/submit` endpoint. The audit gap #3 for the **submit** endpoint is covered.

## 3. M14 commit `2ed0468` — 401 tests for GET/PUT/DELETE widget exist

```
git diff 2ed0468^..2ed0468 -- tests/widgets/test_router.py | grep -n "401_without_auth"
```

Output:
```
     def test_list_widgets_401_without_auth(self, client):
+    def test_get_widget_401_without_auth(self, client):
+    def test_update_widget_401_without_auth(self, client):
+    def test_delete_widget_401_without_auth(self, client):
```

**Verdict:** All three 401-without-auth tests for GET/PUT/DELETE on `/widgets/{id}` were added in M14. The audit gap #1 for these endpoints is covered.
