# M14.1 — Resolve test-count regression & Tier C test hygiene

**Completed:** 2026-07-30  
**Status:** ✅ RESOLVED

---

## 1. Current ci.yml pytest invocation (verbatim)

`.github/workflows/ci.yml:42-65`:

```yaml
python -m pytest \
  tests/embed/ \
  tests/leads/ \
  tests/widgets/ \
  tests/middleware/ \
  tests/models/ \
  tests/repositories/ \
  tests/routers/ \
  tests/scrapers/ \         # ← PRESENT, line 51
  tests/services/ \
  tests/test_main.py \
  tests/test_background_jobs.py \
  tests/test_db_schema.py \
  tests/test_e2e_widget.py \
  tests/test_lead_worker.py \
  tests/test_report_worker.py \
  tests/test_worker.py \
  --ignore=tests/test_e2e.py \
  --ignore=tests/test_ai_e2e.py \
  --cov=app \
  --cov-report=term-missing \
  --tb=short \
  -v
```

`tests/scrapers/` is present at line 51 — **ci.yml was never broken**.

## 2. Root cause: stale AGENTS.md, not a CI regression

`git log --oneline -- .github/workflows/ci.yml`:

```
60c234e Fix CI syntax: revert \\ to \ in run block
0791bdd Fix CI configuration: add missing tests/scrapers/ to pytest command
b0358e2 Update tests and CI configuration
b81bfbf ci: add GitHub Actions workflow
```

No revert or overwrite of `0791bdd`/`60c234e` occurred. Both commits are in the ancestry of all M14 commits. `tests/scrapers/` has been in ci.yml since `0791bdd`.

The problem: **AGENTS.md contained a stale copy** of the pytest command that omitted `tests/scrapers/`. The M14 report used this stale command (`748 collected` vs true `865`).

**Fix applied:** AGENTS.md now matches ci.yml — `tests/scrapers/` line added.

## 3. Corrected literal test output

```
collected 865 items
...
2 failed, 862 passed, 1 xfailed in 49.16s
```

## 4. Coverage (corrected run — all M14 modules 100%)

| Module | Coverage |
|--------|----------|
| `app/routers/embed.py` | 100% |
| `app/routers/leads.py` | 100% |
| `app/routers/widgets.py` | 100% |
| `app/services/lead_service.py` | 100% |
| `app/services/fingerprint_service.py` | 100% |
| `app/services/embed_service.py` | 100% |
| `app/repositories/lead_repo.py` | 100% |
| `app/repositories/widget_repo.py` | 100% |
| **Overall** | **99%** (2603 stmts, 6 pre-existing missing) |

## 5. xfail conversion

`tests/embed/test_router.py:60` now has:

```python
@pytest.mark.xfail(
    reason="Tier C: missing Cache-Control header on config endpoint, "
           "awaiting Ahmed's sign-off — see docs/reviews/m1-architecture.md",
    strict=True,
)
def test_config_cache_control_header(self, client, created_widget):
```

Suite result: **0 unexpected failures** from M14 code. The only failures are the 2 pre-existing Postgres `ConnectionRefusedError` tests.

## 6. Factual claim corrections

| M14 claim | Original | Corrected |
|-----------|----------|-----------|
| Items collected | 748 | **865** (+117 scrapers) |
| Failed | 3 | **2 failed, 1 xfailed** |
| Passed | 745 | **862** |
| Affected module coverage | 100% | ✅ Still 100% |
| Cache-Control absent on config | ✅ Confirmed | ✅ |
| Test names in committed files | ✅ Confirmed | ✅ |
